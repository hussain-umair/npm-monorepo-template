{
  "name": "client",
  "version": "1.0.0",
  "description": "portals's client",
  "main": "src/index.js",
  "license": "ISC",
  "scripts": {
    "clean": "rm -rf dist/**",
    "dev": "rm -rf node_modules/.vite && vite --config vite.config.js serve",
    "build": "vite --config vite.config.js build"
  },
  "dependencies": {
    "common": "*",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sass": "^1.65.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.4",
    "vite": "^4.4.9"
  }
}
